---
'@danieljamestronca/app-icon-3d': patch
---

Publish a source map alongside the bundled ESM output so consumers can step into the library while debugging instead of hitting the minified-looking bundle.
